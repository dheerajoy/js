const countryCode = {
    in: 'India',
    usa: 'America',
    pak: 'Pakistan',
    sin: 'Singapore'
}


for (const key in countryCode) {
    // console.log(`${key} of the ${countryCode[key]}`);
}

const arr = [1,2,3,4,5]

for (const key in arr) {
    // console.log(`${key} is key for value ${arr[key]}`);
}