
// const float = 300.0002;
// console.log(typeof float.valueOf());

// toFixed - returns a fixed decimal number in range 0 - 20 as string 
// const fractionDigits = score.toFixed(2)
// console.log(typeof fractionDigits);

// const score = new Number(1000000);

// console.log(score.toLocaleString('en-In')); 



//++++++++++ maths ++++++++++++++

// console.log(Math);

// abs
// console.log(Math.abs(-23)); // 23
// converts - to +

// round
// console.log(Math.round(1.4)); // 1
// returns round off value to its nearest integer


// random 
// console.log(Math.random());
// Returns a pseudorandom number between 0 and 1.

// max
// console.log(Math.max(1,2,6,2,10,2,9));
// Returns the larger of a set of supplied numeric expressions.

// power
// console.log(Math.pow(2,3));
// returns the value of a base expression taken to a specified power.
// The base value of the expression.
// The exponent value of the


console.log(Math.random());
console.log(Math.random() * 10 + 1);


const min = 0o1;
const max = 9999;

console.log(Math.floor((Math.random() * (max - min + 1)) + min));

